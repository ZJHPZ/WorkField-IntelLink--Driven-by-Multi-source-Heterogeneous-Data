# 职域智联 - AI职业领航员「帕克」接入文档

## 1. 智能体简介

「职域智联」是一个 AI 职业领航员智能体（呼号：帕克），基于用户真实数据提供精准、可量化的职业规划建议。

**核心能力：**
- 技能画像分析与人岗匹配评估
- 学习路径规划与技能保鲜诊断
- 转行可行性分析与岗位探索
- 简历优化建议与职业演化趋势分析

**交互风格：** 使用航空隐喻（航线/引擎/燃料等），冷静精确，Markdown 格式输出。

---

## 2. 接口信息

| 项目 | 值 |
|------|------|
| **请求地址** | `https://886xv55s2h.coze.site/stream_run` |
| **请求方法** | `POST` |
| **Content-Type** | `application/json` |
| **响应格式** | `text/event-stream`（SSE 流式响应） |

### 2.1 认证方式

在请求头中携带 Bearer Token：

```
Authorization: Bearer <API_TOKEN>
```

**API Token：**
```
eyJhbGciOiJSUzI1NiIsImtpZCI6IjZkYWM4ODE3LTAxZWMtNGJjNS1iY2VjLWNhNDFjNDNlMDYxOCJ9..c84wfx6iaUYPvjlxaAyOSyHPyffTdRHfNfwiv2u5U_Zi3P-VdxwbmQnNXG4JxHpox_W8-_60DHxLWoGK7XxwJZuhP5D2ViAN6hnZxBtBwknGix9RwYY8Ot70gl5l0cOIxcOoEoBO0D8nMQL8PH8eYvn77jqNTEuxbdzDtE2X80D33eo-ywdNhYAqM4BKGdJ7QDWGCgR4R7i7nnhy55mroKz3RhKFB4VTCQmxjBeFol4VfeibzJg_3rJPfWaYl40cCSOwe7mWQpPwfbQ8RYCMNXgha3Hbe3EjJX8z1ZIINYjfTEz4S_PoCnI2yB7ZRMzMYklmQ4CrEFIqWnsVR6vGBw
```

> **安全提示：** 请妥善保管 Token，不要在客户端代码或公开仓库中硬编码。建议通过环境变量或服务端代理传递。

---

## 3. 请求格式

### 3.1 请求体结构

```json
{
  "type": "query",
  "project_id": "",
  "session_id": "<会话ID，用于多轮对话>",
  "local_msg_id": "<消息ID，可选>",
  "content": {
    "query": {
      "prompt": [
        {
          "type": "text",
          "content": {
            "text": "用户输入的文本内容"
          }
        }
      ]
    }
  }
}
```

### 3.2 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | string | 是 | 消息类型，固定为 `"query"` |
| `project_id` | string | 否 | 项目 ID，可留空 |
| `session_id` | string | 是 | 会话 ID，用于多轮对话上下文关联。同一会话保持相同 session_id |
| `local_msg_id` | string | 否 | 消息唯一 ID，可留空（服务端会自动生成） |
| `content.query.prompt` | array | 是 | 消息内容块数组，至少包含一个 text 类型的块 |
| `content.query.prompt[].type` | string | 是 | 内容块类型，文本为 `"text"` |
| `content.query.prompt[].content.text` | string | 是 | 用户输入的文本内容 |

### 3.3 请求示例

#### 单轮对话

```json
{
  "type": "query",
  "session_id": "session-001",
  "content": {
    "query": {
      "prompt": [
        {
          "type": "text",
          "content": {
            "text": "我叫张明，前端开发5年，北京，想转型AI工程师。掌握React/Vue/TypeScript，缺少Python和机器学习。匹配率45%，差距技能：Python、机器学习、深度学习框架。请帮我做职业规划。"
          }
        }
      ]
    }
  }
}
```

#### 多轮对话（第二轮）

```json
{
  "type": "query",
  "session_id": "session-001",
  "content": {
    "query": {
      "prompt": [
        {
          "type": "text",
          "content": {
            "text": "如果我想先学Python，有什么推荐的学习路径？"
          }
        }
      ]
    }
  }
}
```

> **多轮对话说明：** 保持 `session_id` 不变即可实现上下文连续对话。智能体默认保留最近 20 轮对话记忆。

---

## 4. 响应格式

### 4.1 响应类型

接口返回 **SSE（Server-Sent Events）** 流式响应，`Content-Type: text/event-stream`。

### 4.2 SSE 事件格式

每个事件由以下格式组成：

```
event: message
data: <JSON数据>

```

### 4.3 事件数据类型

响应流中包含以下几种事件类型：

#### (1) 消息开始事件

```json
{
  "type": "message_start",
  "content": {
    "message_start": {
      "session_id": "session-001",
      "local_msg_id": "msg-xxx"
    }
  }
}
```

#### (2) 文本回答事件（流式输出）

```json
{
  "type": "answer",
  "content": {
    "answer": {
      "text": "雷达扫描完成，张明机长..."
    }
  }
}
```

> 多个 `answer` 事件会连续推送，将 `text` 字段拼接即为完整回答。

#### (3) 消息结束事件

```json
{
  "type": "message_end",
  "content": {
    "message_end": {
      "code": 0,
      "message": "success"
    }
  }
}
```

> `code` 为 `0` 表示成功，非 `0` 表示异常。

---

## 5. 接入代码示例

### 5.1 Python 示例

```python
import requests
import json

API_URL = "https://886xv55s2h.coze.site/stream_run"
API_TOKEN = "eyJhbGciOiJSUzI1NiIsImtpZCI6IjZkYWM4ODE3LTAxZWMtNGJjNS1iY2VjLWNhNDFjNDNlMDYxOCJ9..c84wfx6iaUYPvjlxaAyOSyHPyffTdRHfNfwiv2u5U_Zi3P-VdxwbmQnNXG4JxHpox_W8-_60DHxLWoGK7XxwJZuhP5D2ViAN6hnZxBtBwknGix9RwYY8Ot70gl5l0cOIxcOoEoBO0D8nMQL8PH8eYvn77jqNTEuxbdzDtE2X80D33eo-ywdNhYAqM4BKGdJ7QDWGCgR4R7i7nnhy55mroKz3RhKFB4VTCQmxjBeFol4VfeibzJg_3rJPfWaYl40cCSOwe7mWQpPwfbQ8RYCMNXgha3Hbe3EjJX8z1ZIINYjfTEz4S_PoCnI2yB7ZRMzMYklmQ4CrEFIqWnsVR6vGBw"

def chat_with_parker(user_message: str, session_id: str = "default-session") -> str:
    """与职业领航员帕克对话"""
    headers = {
        "Authorization": f"Bearer {API_TOKEN}",
        "Content-Type": "application/json",
    }

    payload = {
        "type": "query",
        "session_id": session_id,
        "content": {
            "query": {
                "prompt": [
                    {
                        "type": "text",
                        "content": {"text": user_message}
                    }
                ]
            }
        }
    }

    response = requests.post(API_URL, headers=headers, json=payload, stream=True)
    response.raise_for_status()

    full_answer = ""
    for line in response.iter_lines(decode_unicode=True):
        if not line or not line.startswith("data: "):
            continue
        data_str = line[len("data: "):]
        try:
            data = json.loads(data_str)
        except json.JSONDecodeError:
            continue

        if data.get("type") == "answer":
            text = data.get("content", {}).get("answer", {}).get("text", "")
            full_answer += text
            print(text, end="", flush=True)  # 实时打印

        elif data.get("type") == "message_end":
            code = data.get("content", {}).get("message_end", {}).get("code", -1)
            if code != 0:
                print(f"\n[错误] 响应异常，code={code}")

    return full_answer


# 使用示例
if __name__ == "__main__":
    print("=== 职域智联 - AI职业领航员帕克 ===\n")

    # 第一轮对话
    query = (
        "我叫张明，前端开发5年经验，在北京，想转型AI工程师。"
        "掌握React/Vue/TypeScript，缺少Python和机器学习。"
        "匹配率45%，差距技能：Python、机器学习、深度学习框架。"
        "请帮我做职业规划分析。"
    )
    answer = chat_with_parker(query, session_id="session-zhangming")
    print("\n")

    # 第二轮对话（追问）
    follow_up = "你觉得我转型最大的风险是什么？有什么备选方案？"
    answer2 = chat_with_parker(follow_up, session_id="session-zhangming")
```

### 5.2 cURL 示例

```bash
curl -X POST "https://886xv55s2h.coze.site/stream_run" \
  -H "Authorization: Bearer <API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "type": "query",
    "session_id": "session-001",
    "content": {
      "query": {
        "prompt": [
          {
            "type": "text",
            "content": {
              "text": "你好，请介绍一下你能提供哪些职业规划服务？"
            }
          }
        ]
      }
    }
  }'
```

### 5.3 JavaScript / Node.js 示例

```javascript
const API_URL = "https://886xv55s2h.coze.site/stream_run";
const API_TOKEN = "<YOUR_API_TOKEN>";

async function chatWithParker(userMessage, sessionId = "default-session") {
  const response = await fetch(API_URL, {
    method: "POST",
    headers: {
      Authorization: `Bearer ${API_TOKEN}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      type: "query",
      session_id: sessionId,
      content: {
        query: {
          prompt: [{ type: "text", content: { text: userMessage } }],
        },
      },
    }),
  });

  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  let fullAnswer = "";

  while (true) {
    const { done, value } = await reader.read();
    if (done) break;

    const chunk = decoder.decode(value, { stream: true });
    const lines = chunk.split("\n");

    for (const line of lines) {
      if (!line.startsWith("data: ")) continue;
      const dataStr = line.slice(6);
      try {
        const data = JSON.parse(dataStr);
        if (data.type === "answer") {
          const text = data.content?.answer?.text || "";
          fullAnswer += text;
          process.stdout.write(text); // 实时输出
        }
      } catch (e) {
        // 忽略解析错误
      }
    }
  }
  return fullAnswer;
}

// 使用示例
chatWithParker(
  "我是数据分析师，3年经验，想转型AI算法工程师，请帮我分析职业规划。",
  "session-001"
);
```

---

## 6. 最佳实践

### 6.1 输入建议

为获得最精准的职业规划建议，建议用户输入包含以下信息：

| 信息类别 | 字段 | 示例 |
|---------|------|------|
| **基础信息** | 姓名、当前职位、工作经验、所在城市 | "我叫李明，产品经理，5年经验，深圳" |
| **目标岗位** | 明确的目标职位 | "想转型为AI产品经理" |
| **技能画像** | 掌握技能、缺口技能 | "掌握Axure/SQL，缺少机器学习基础" |
| **匹配数据** | 匹配率、差距技能 | "匹配率55%，差距：深度学习、模型评估" |

### 6.2 多轮对话

- 使用相同的 `session_id` 维持对话上下文
- 智能体保留最近 **20 轮** 对话记忆
- 如需开始新的独立咨询，使用新的 `session_id`

### 6.3 错误处理

```python
# 推荐的错误处理模式
try:
    response = requests.post(API_URL, headers=headers, json=payload, stream=True, timeout=120)
    response.raise_for_status()
    
    for line in response.iter_lines(decode_unicode=True):
        if line.startswith("data: "):
            data = json.loads(line[6:])
            if data.get("type") == "message_end":
                code = data["content"]["message_end"]["code"]
                if code != 0:
                    print(f"智能体返回异常: code={code}")
                    break
except requests.exceptions.Timeout:
    print("请求超时，请稍后重试")
except requests.exceptions.ConnectionError:
    print("网络连接失败，请检查网络")
except Exception as e:
    print(f"请求异常: {e}")
```

### 6.4 超时设置

- 建议请求超时设置为 **120 秒**（职业规划分析涉及深度推理）
- 流式响应首 token 通常在 3-10 秒内返回

---

## 7. 智能体边界说明

| 能力范围 | 说明 |
|---------|------|
| ✅ 职业规划分析 | 基于用户数据的个性化职业建议 |
| ✅ 技能差距分析 | 识别并量化技能缺口 |
| ✅ 学习路径推荐 | 带优先级和时间规划的学习方案 |
| ✅ 转行可行性评估 | 转型难度、周期、资源分析 |
| ❌ 非职业领域问题 | 智能体会礼貌拒绝并引导回职业话题 |
| ❌ 就业结果承诺 | 仅提供建议，不承诺任何就业结果 |
| ❌ 无数据编造 | 当缺少数据时会明确告知，不会编造信息 |

---

## 8. 技术栈信息

| 组件 | 技术 |
|------|------|
| 大语言模型 | 讯飞星火 Spark-X2-Flash |
| Agent 框架 | LangGraph 1.0 |
| 对话记忆 | 滑动窗口（20轮/40条消息） |
| 部署方式 | SSE 流式响应 |
